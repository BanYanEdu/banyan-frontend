export declare class DateUtils {
    private static datePipe;
    static DATETIME_FORMAT: string;
    static VN_DATETIME_FORMAT: string;
    static DATE_FORMAT: string;
    static TIME_FORMAT: string;
    static format(date: Date | number | string, format?: string): string;
}
