export declare let AjaxAPI: {
    getUrl(url: string, domain?: string, firmPrefix?: string): string;
    getPUrl(url: string, domain?: string, firmPrefix?: string): any;
    sendRequest(options: any): any;
    postForm(options: any): any;
};
