import { WebSocketAbstract, WebSocketEnvelop } from './websocket-abstract';
export declare class WebSocketVertx extends WebSocketAbstract {
    private _eventbus;
    readonly readyState: number;
    connect(options: any, username: string): void;
    private _connect;
    close(): void;
    send(envelop: WebSocketEnvelop): Promise<boolean>;
}
